const people=[{name: 'Pranjali', age: 18},
{name :'PRIYA',age :19},
{name:'Hello',age :20}];

const names={};
for (let index in people)
{
   const {name}=people[index];
   names{}=name;
 }
 console.log(names);

